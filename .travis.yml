language: node_js
node_js:
  - "node"
env:
  - RAFR_DB_HOST=localhost RAFR_DB_NAME=rafr NODE_ENV=development
services:
  - mysql
install:
  - cd ./API/
  - npm install
  - cd ../
before_script:
  - mysql -u root -e $RAFR_ALTER_USER_SQL
  - mysql -u $RAFR_DB_USERNAME -p$RAFR_DB_PASSWORD < ./models/schema.sql
script:
  - npm run generate-models
  - npm test
